version: '3'

services:
  db:
    image: postgres
    ports:
      - "5432:5432"

  api:
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      STAGE: "2"
    depends_on:
      - db
